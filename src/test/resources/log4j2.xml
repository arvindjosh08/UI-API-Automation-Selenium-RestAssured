<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <!-- ============================= -->
    <!-- Dynamic log folder properties -->
    <!-- ============================= -->
    <Properties>
        <!-- Base log folder relative to project root -->
        <Property name="LOG_BASE">${sys:log.folder}</Property>
        <!-- Timestamped folder per run for CI/CD and parallel execution -->
        <Property name="LOG_FOLDER">${LOG_BASE}/${date:yyyyMMdd_HHmmss}</Property>
    </Properties>

    <!-- ============================= -->
    <!-- Appenders -->
    <!-- ============================= -->
    <Appenders>
        <!-- Console appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Rolling file appender -->
        <RollingFile name="FileLogger"
                     fileName="${LOG_FOLDER}/framework.log"
                     filePattern="${LOG_FOLDER}/framework-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</pattern>
            </PatternLayout>
            <Policies>
                <!-- Roll every day -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <!-- Roll when file size exceeds 10 MB -->
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
        </RollingFile>
    </Appenders>

    <!-- ============================= -->
    <!-- Loggers -->
    <!-- ============================= -->
    <Loggers>
        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Root>

        <!-- Framework-specific logger for debug messages -->
        <Logger name="com.company.framework" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
        </Logger>
    </Loggers>
</Configuration>